# Publishes the app to GitHub Pages.
#
# Runs only after `verify` has passed on master, so a broken build cannot become
# the live site. That ordering is the whole point of having it as a separate
# workflow rather than another job in the same one.
name: deploy

on:
  workflow_run:
    workflows: ["verify"]
    branches: [master]
    types: [completed]
  # Available by hand for a re-deploy that does not need a new commit.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One deployment at a time, and never cancel one in flight. Interrupting a
# deploy can leave the site serving half of two builds.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    # Only when verify actually succeeded. A workflow_run fires on failure too.
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - run: npm ci

      # Uploads need an Imgur Client-ID. It is a public identifier, not a
      # secret, so it lives in repository variables rather than secrets. Without
      # it the upload button is not shown and pasting an address is the whole
      # feature, which is how the gallery was built in the first place.
      - run: npm run build:app
        env:
          VITE_IMGUR_CLIENT_ID: ${{ vars.IMGUR_CLIENT_ID }}

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: app/dist

      - id: deployment
        uses: actions/deploy-pages@v4
